AMQP_URL=amqp://guest:guest@localhost:5672

# nome do exchange que o payment publica
EVENTS_EXCHANGE=payments.x

# quantas tentativas de retry antes de mandar pra DLQ
MAX_RETRIES=3

# atraso base do retry (ms). 
RETRY_DELAY_MS_BASE=1000
